# ChatGPT Export

Use this source when the user wants to index a ChatGPT data export.

Prefer:

```bash
leann index --source chatgpt-export
```

Use `leann index-chatgpt --export-path <path>` only as the deprecated
compatibility alias.
