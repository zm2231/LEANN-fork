# Claude Export

Use this source when the user wants to index a Claude data export.

Prefer:

```bash
leann index --source claude-export
```

Use `leann index-claude --export-path <path>` only as the deprecated
compatibility alias.
