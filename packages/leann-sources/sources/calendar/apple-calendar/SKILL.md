# Apple Calendar

Use this source when the user wants to index local macOS Calendar events.

Prefer:

```bash
leann index --source apple-calendar
```

Run `leann sources validate apple-calendar` first when the Calendar cache is
missing or Full Disk Access is uncertain.
