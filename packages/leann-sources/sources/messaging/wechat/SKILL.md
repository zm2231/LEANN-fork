# WeChat Export

Use this source when the user wants to index a local WeChat JSON export.

Prefer:

```bash
leann index --source wechat
```

Use `leann index-wechat --export-dir <dir>` only as the deprecated
compatibility alias.
