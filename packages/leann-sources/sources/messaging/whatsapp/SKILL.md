# WhatsApp Source

Use this source when the user wants to index WhatsApp message history from an exported or backup-extracted `ChatStorage.sqlite` database.

Prefer `leann sources validate whatsapp` before indexing because most failures are missing backup files, not parser errors.
