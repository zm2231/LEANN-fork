# Apple Mail

Use this source when the user wants to index local Apple Mail history from
macOS Mail storage.

Prefer:

```bash
leann index --source apple-mail
```

Run `leann sources validate apple-mail` first when Mail storage may be missing
or the terminal lacks Full Disk Access.
