# GitHub Source

Use this source when the user wants LEANN to index GitHub repository activity such as issues, pull requests, comments, or commits.

Check `GITHUB_TOKEN` and the repository list before indexing. Use fixture tests for parser changes; live API runs should be explicit because they consume rate limit.
