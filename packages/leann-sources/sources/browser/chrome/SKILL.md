# Chrome History

Use this source when the user wants to index local Chrome or Brave browsing
history.

Prefer:

```bash
leann index --source chrome
```

Use `leann index-browser brave` only for backwards-compatible Brave profile
indexing.
